openIdSettings:
  authorizationEndpoint: "https://accounts.google.com/o/oauth2/v2/auth"
  clientId: "Google OpenID client_id"
  redirectUri: "https://localhost"
  responseType: "code"
  scope:
    - openid
    - email
